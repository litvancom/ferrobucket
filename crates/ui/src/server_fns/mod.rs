//! Leptos server functions (#[server]) for the web UI.
//! All storage access goes through these server-side functions — never in WASM (DEC-ui-ssr).
//! Submodules: buckets, objects, presign, settings.
//! Filled by Plan 02 (Wave 0+).
