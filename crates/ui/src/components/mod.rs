//! SSR-only reusable UI components: bucket table, object table, breadcrumb, empty state, etc.
//! These components are never compiled to WASM — rendered server-side only.
//! Filled by Plan 05 (Wave 1).
